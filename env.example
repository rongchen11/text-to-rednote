# 环境变量配置示例
# 复制此文件为 .env.local 并填入真实值

# 支付提供商配置
# 可选值: 'zpay' | 'creem'
VITE_PAYMENT_PROVIDER=creem

# Z-Pay 商户配置
ZPAY_PID=2025062920440492
ZPAY_KEY=tNeFjVxC3b8IlgNJvqFA9oRNxy9ShaA1

# Creem 支付配置（后端使用 - 敏感信息）
# ⚠️ 重要：以下配置仅在后端使用，不要添加 VITE_ 前缀！
CREEM_API_KEY=creem_4yH5Q3DM2HuXRGaOJmpJiw
CREEM_WEBHOOK_SECRET=whsec_7Zer79qbRSW39bKcx4QPXf

# 应用配置
VITE_APP_URL=https://www.rednotewriter.com
VITE_SITE_NAME=文字转RedNote

# 启用真实支付
ZPAY_DEMO_MODE=false

# 豆包API配置（图片生成）
VITE_DOUBAO_API_KEY=your-doubao-api-key

# Supabase 配置（如果需要）
VITE_SUPABASE_URL=https://placeholder.supabase.co
VITE_SUPABASE_ANON_KEY=placeholder-anon-key

# 说明：
# 1. 将此文件复制为 .env.local（本地开发）
# 2. 在 Vercel 中配置环境变量（生产环境）
# 
# ⚠️ 安全重要提示：
# - CREEM_API_KEY 和 CREEM_WEBHOOK_SECRET 不要添加 VITE_ 或 NEXT_PUBLIC_ 前缀
# - 这些敏感信息只在后端使用，添加前缀会暴露到前端代码中！
# - 在 Vercel 环境变量中，也不要添加 VITE_ 前缀
#
# 3. Creem 支付（推荐）：后端配置 CREEM_API_KEY 和 CREEM_WEBHOOK_SECRET
# 4. Z-Pay 支付：配置 ZPAY_PID 和 ZPAY_KEY
# 5. 默认使用 Creem 支付（VITE_PAYMENT_PROVIDER=creem）
